<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
  <div class="container">
    <div class="navbar-header page-scroll">
      <a class="navbar-brand page-scroll" href=<%= if params[:id] then "/?category_id="+params[:id] else "/" end%>>육아는 아이템전</a>
    </div>
    <ul class="nav navbar-nav navbar-right">
      <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= current_category_name %> <span class="caret"></span></a>
        <ul class="dropdown-menu">
          <% categories.each do |category| %>
            <li><a href=<%= category_path(category) %>><%= category.name %></a></li>
          <% end %>
        </ul>
      </li>
    </ul>
  </div>
</nav>